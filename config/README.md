# Config Templates

Status: starting baseline.

Store sanitized config templates and examples here.

This folder is for deployment inputs, not unsanitized snapshots. Sanitized known-good snapshots belong in `edsys-infra-configs`.

Rules:

- Use placeholders only.
- Explain where real values should be supplied.
- Do not commit secrets.
