table inet edsys_voice_gateway {
    chain forward {
        type filter hook forward priority filter - 1; policy accept;

        iifname "enp7s0" ip saddr 192.168.50.75 tcp dport 8443 ct original proto-dst 8055 counter accept
        tcp dport 8443 ct original proto-dst 8055 counter drop
    }
}
